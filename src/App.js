import './App.css';
import React from 'react';
import Chatbot from './Chatbot'; // Chatbot 컴포넌트를 임포트합니다.

const App = () => {
  return (
    <div>
      <Chatbot /> {/* Chatbot 컴포넌트를 렌더링합니다. */}
    </div>
  );
};

export default App; // App 컴포넌트를 내보냅니다.
